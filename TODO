+ manually added aliases

- fixup steps

 - peoples' changed email addresses - old => new
 (not relevant?)

 + for old, closed WGs - ADs should be still current ADs
  + should do it probably all the time anyway?
